const scene=new THREE.Scene,geometry=new THREE.BoxGeometry(1,1,1),material=new THREE.MeshBasicMaterial({color:16711680}),mesh=new THREE.Mesh(geometry,material);scene.add(mesh);const sizes={width:800,height:600},camera=new THREE.PerspectiveCamera(75,sizes.width/sizes.height);camera.position.z=3,scene.add(camera);const canvas=document.querySelector("canvas.webgl"),renderer=new THREE.WebGLRenderer({canvas});renderer.setSize(sizes.width,sizes.height),renderer.render(scene,camera);